
using static ThanaNita.Turtles.One;

//ApplicationConfiguration.Initialize();

Speed = 800;
PenColor = Color.Blue;
for (int i = 0; i < 20; ++i)
{
    Forward(200);
    TurnRight(88);
}
Fill(Color.Yellow);

